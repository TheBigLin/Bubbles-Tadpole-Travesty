using UnityEngine;

public class areatrigger : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    
    public string title = "title ??";
    public string Auther = "by: ???";
}
